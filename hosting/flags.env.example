# Real flags for the hosted instances. Keep this file private (gitignored).
# Students receive these from /claim only after Setup.isSolved() is true.
MULTISIG_FLAG=boiler{REPLACE_ME}
FLASHCRASH_FLAG=boiler{REPLACE_ME}
DOUBLEDOWN_FLAG=boiler{REPLACE_ME}
