# Postgres connection string (Neon on Vercel, or any Postgres).
DATABASE_URL=

# Origin phones will open when they scan a QR or tap an NFC tag.
# Production: https://attendance.boilerblockchain.org
# Local dev: leave unset so QR links fall back to the request host (use your
# LAN IP, e.g. http://192.168.1.20:3000, not localhost, so phones can reach it).
NEXT_PUBLIC_APP_URL=

# WalletConnect (Reown) project id, from https://cloud.reown.com. Without it
# the app can only connect a desktop browser extension, so phones cannot check
# in at all. This value is public by design and is compiled into the client
# bundle: never put a Reown *secret* here. Changing it requires a rebuild.
NEXT_PUBLIC_WC_PROJECT_ID=

# Stable HMAC key for officer session cookies. Without it the app derives one
# from DATABASE_URL, which rotates every time the database URL changes.
OFFICER_SESSION_SECRET=

# Chain the check-in signature is scoped to. Nothing is written on-chain.
CHAIN_ID=84532
NEXT_PUBLIC_CHAIN_ID=84532
RPC_URL=https://sepolia.base.org

# Token lifetimes (seconds). Defaults shown.
# SESSION_TOKEN_TTL_SECONDS is the shared QR session (rotates at half of it).
# NFC_SESSION_TTL_SECONDS is the single-use token minted by one tag tap.
SESSION_TOKEN_TTL_SECONDS=240
NFC_SESSION_TTL_SECONDS=120
CHALLENGE_TTL_SECONDS=300
